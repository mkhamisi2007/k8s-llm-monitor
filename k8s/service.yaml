apiVersion: v1
kind: Service
metadata:
  name: cluster-monitor-service
  namespace: cluster-monitor
  labels:
    app: cluster-monitor
spec:
  type: NodePort
  selector:
    app: cluster-monitor
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
